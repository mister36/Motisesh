import React from 'react';
import {View, StyleSheet, Text} from 'react-native';

const MotiMessageScreen = () => {
  return <View />;
};

const styles = StyleSheet.create({});

export default MotiMessageScreen;
